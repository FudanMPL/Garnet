# program.use_trunc_pr = True
import Compiler.mpc_math
import random
import ml
# n = 1
# cnt = 0

c = sfix(0.5)
d = ml.relu(c)

# a = sfix(0,5)
# b = sfix(0.5)

# c = a*b
# a = sfix.Array(n)
# b = sfix.Array(n)
# c = sfix.Array(n)
# for i in range(n):
#     a[i] = random.randint(-10000, 10000)/100
#     b[i] = random.randint(-10000, 10000)/100

# for i in range(n):
#     c[i] = (a[i] > b[i])
    # clear_a = a[i].reveal()
    # clear_b = b[i].reveal()
    # d = (clear_a > clear_b)
    # if c[i].reveal() != d:
    #     print_ln("a is %s, b is %s", a[i].reveal(), b[i].reveal())
    #     print_ln("c is %s , d is %s, a-b is %s", c[i].reveal(), d, (a[i]-b[i]).reveal()) 
"""
import Compiler.mpc_math
import random
import math

n = 100
cnt = 0

for i in range(n):
    a = sfix(-12942)
    b = sfix(12039)
    c = (a > b)
    clear_a = a.reveal()
    clear_b = b.reveal()
    d = (clear_a > clear_b)
    if c.reveal() != d:
        print_ln("a is %s, b is %s", a.reveal(), b.reveal())
        print_ln("c is %s , d is %s, a-b is %s", c.reveal(), d, (a-b).reveal())    
"""