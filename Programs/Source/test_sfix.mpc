import Compiler.mpc_math
import random
import math

n = 100
cnt = 0

A = sfix.Array(n)
B = sfix.Array(n)
C = sfix.Array(n)
for i in range(n):
    A[i] = sfix.get_random(-500,500)
    B[i] = sfix.get_random(-500,500)

for i in range(n):
    C[i] = (A[i] > B[i])
    clear_a = A[i].reveal()
    clear_b = B[i].reveal()
    d = (clear_a > clear_b)
    if(C[i].reveal() != d):
       print_ln("c is %s , a-b is %s", C[i].reveal(), (A[i]-B[i]).reveal())